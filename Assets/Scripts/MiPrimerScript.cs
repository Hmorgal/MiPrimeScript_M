using UnityEngine;

public class MiPrimerScript : MonoBehaviour
{

    public int numero = 6;
    float decimales = 5.14f;
    bool fin = false;
    string texto = "amogus";

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        print(numero);
    }

    // Update is called once per frame
    void Update()
    {
        print("¿Qué tal?");
    }
}
